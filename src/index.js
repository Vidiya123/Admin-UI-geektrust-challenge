import React from "react";
import ReactDOM from "react-dom";
import GeekTrust from "./App";

ReactDOM.render(
  <React.StrictMode>
    <GeekTrust />
  </React.StrictMode>,
  document.getElementById("root")
);
